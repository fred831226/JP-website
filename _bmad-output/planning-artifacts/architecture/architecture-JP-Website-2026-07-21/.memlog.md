---
scope: JP PUMP V1 public website, content operations, governed product data, and production delivery
purpose: Build substrate for implementation and operations
altitude: initiative
updated: 2026-07-21T23:29
---

- (constraint) Greenfield repository: no application code or project-context.md exists; the architecture may establish the cold-start structure.
- (constraint) [ADOPTED] Next.js is the required public frontend foundation; no second frontend framework is precommitted.
- (constraint) The product has two ownership lanes: the client administrator owns News, Projects, and their media; the designated maintainer owns products, taxonomy, partners, company/contact facts, Hero media, and governed releases. A datum must never be authoritative in both lanes.
- (constraint) V1 publishes only Traditional Chinese under /zh-tw/; future /en/ support must be structurally possible but no empty or machine-filled English route may exist.
- (constraint) Public product data requires source, rights, review provenance, stable identity, last-updated date, missing-value preservation, and publish blocking on major integrity errors. Internal reviewer names and evidence never enter public DTOs.
- (constraint) Catalog validation must use about 603 models, the approximately 491-model SB/SBI/SBN series, and the cross-type VBSG series; a short sample is not an acceptance proxy.
- (constraint) V1 has no public login, contact form, automated selection, quotation, transaction, customer product CRUD, permanent Excel import UI, or client-managed product/partner/Hero collections.
- (constraint) Release floors are WCAG 2.2 AA, 320 CSS px without page-level horizontal overflow, p75 LCP 2.5s or less, INP 200ms or less, CLS 0.1 or less, 99.9% monthly availability target, RTO 4h, and RPO 24h.
- (direction) CMS alternatives researched from official sources. Payload 3.86 offers Next.js-native installation, granular access, auth/recovery, drafts, versions, Postgres, and official storage adapters. Sanity reduces infrastructure but separates product release tooling and limits roles/backups/release rollback by plan. Decap is portable but turns drafts into Git branches/PRs and is a weaker fit for the nontechnical media and recovery journey. Lean Payload while retaining export/exit contracts.
- (assumption) [ASSUMPTION] Use a Jamstack-oriented hexagonal modular monolith: one Next.js/Payload deployable, domain/application modules inward, Payload/Git/Blob/analytics adapters outward, and public plus admin route groups as delivery surfaces. This keeps the official starter path while preventing CMS types from becoming domain contracts.
- (assumption) [ASSUMPTION] Seed from the official Payload Website template, remove page-builder/form/ecommerce features, and keep only auth, access control, drafts/versions, redirects, fixed-schema News/Projects/Media, and on-demand revalidation.
- (assumption) [ASSUMPTION] Deploy the commercial production app to Vercel Pro, use Neon Launch Postgres with at least 24h and preferably 7d point-in-time restore, Vercel Blob for immutable web media, and Resend SMTP for administrator password recovery. Estimated baseline is roughly USD 35-60/month before domain and optional monitoring overages.
- (version) Verified 2026-07-21 production seed: Node.js 24.17.0 LTS; Next.js 16.2.10 latest stable; React/React DOM 19.2.7; Payload 3.86.0; pnpm 11.15.1. Payload officially supports Next.js 16.2.6 and newer.
- (version) Verified 2026-07-21 supporting seed: Tailwind CSS 4.3.3, React Aria Components 1.19.0, Zod 4.4.3, Playwright 1.61.1, @axe-core/playwright 4.12.1. TypeScript latest is 7.0.2 but must use the exact version proven by the chosen Payload starter/CI rather than adopting a newly released compiler blindly.
- (assumption) [ASSUMPTION] Public pages are Server Components and pre-rendered/ISR by default; client components are limited to navigation, URL-backed filters, the finite Hero sequence, disclosures, and image dialog. No general client state store or client data-fetch cache is introduced.
- (assumption) [ASSUMPTION] News and Project documents live in Payload/Postgres. Product taxonomy, products/models, partners, company/contact facts, controlled redirects, and governed media manifests live as immutable Git-versioned release bundles. Payload may read stable Series IDs for relationships but is never their source of truth.
- (assumption) [ASSUMPTION] Every catalog release is a normalized immutable snapshot with schemaVersion, releaseVersion, content hash, source refs, maintainer, offline technical reviewer/date, and four dispositions. CI diff/validation gates precede a preview deployment; merging publishes code and catalog atomically; rollback promotes the previous known-good deployment.
- (assumption) [ASSUMPTION] Stable opaque IDs are independent from locale slugs. Decimal specifications are strings plus explicit unit enums; missing is null and renders 未提供. ISO 8601 UTC timestamps cross boundaries. Public DTOs omit rights evidence, reviewer identity, source documents, and drafts.
- (assumption) [ASSUMPTION] CMS media enters a private source store with alt/source/rights metadata; only approved optimized derivatives are promoted to immutable public Blob paths. Governed product/Hero/partner media uses the same private-source to public-derivative rule through maintainer scripts and a Git manifest.
- (assumption) [ASSUMPTION] Use semantic CSS custom properties as the design-token authority, Tailwind as the constrained composition layer, React Aria Components only for complex interaction primitives, and native HTML for links, selects, tables, details, and buttons wherever it satisfies the UX contract.
- (assumption) [ASSUMPTION] Filter state authority is a validated canonical query string. Values are sorted/deduplicated; within-dimension OR and cross-dimension AND are server-owned rules. Filtered URLs emit noindex,follow and stay out of sitemap; curated category pages use clean self-canonical URLs.
- (assumption) [ASSUMPTION] Render the full approximately 491-row model table as server HTML with no hydration or virtualization; only its labelled container scrolls horizontally. Compress, measure, and fail the performance gate if the real largest release violates budgets before considering explicit pagination.
- (assumption) [ASSUMPTION] Production, Preview, and Development are isolated. Production uses production DB/blob/secrets; all preview deployments use a shared non-production DB/blob and robots noindex plus deployment access protection. Preview code must never write to production data.
- (assumption) [ASSUMPTION] Postgres migrations are generated and committed, expand/migrate/contract, and backward compatible with the previous deployment. No destructive migration runs automatically; take a recovery point and require explicit maintenance approval first.
- (assumption) [ASSUMPTION] Recovery uses provider PITR plus nightly logical DB export, immutable asset inventories and off-provider asset backup, deployment rollback, and a quarterly restore drill. These controls bind to RPO 24h and RTO 4h rather than relying on provider durability alone.
- (assumption) [ASSUMPTION] Browser analytics are consent-gated through one TelemetryPort; server logs and errors are structured and redacted; one external monitor outside Vercel checks a minimal health endpoint. Provider choice for consent/error/RUM is deferred until privacy and budget confirmation.
- (assumption) [ASSUMPTION] CI gates include typecheck/lint, schema and catalog validation, CMS access/publish-hook tests, Playwright journeys across Chromium/WebKit/Firefox, axe checks, 320px and largest-series checks, link/redirect/sitemap checks, dependency vulnerability review, and production build.
- (question) BLOCKER TO ADOPTION: Is a recurring production budget around USD 35-60/month acceptable for Vercel Pro plus Neon Launch, email, storage usage, and basic external monitoring? If not, re-open hosting/CMS trade-offs.
- (question) BLOCKER TO PROVISIONING: Should JP PUMP own the GitHub, Vercel, Neon, email, DNS/domain, GA4, Search Console, and monitoring organizations with the maintainer delegated access? Recommendation: yes; no production account should be owned only by a private maintainer identity.
- (question) Before production provisioning, supply the administrator recovery mailbox, alert recipients, domain/registrar state, and the approved off-provider backup destination. These do not block the draft spine.
- (question) Before enabling GA4/RUM, confirm the applicable consent model and retention policy. Core public functions remain independent from consent and tracking.
- (event) Async coaching research pass completed; Payload/Vercel/Neon direction remains an assumption pending user review.
- (version) Vercel Private Blob reached general availability on 2026-06-30; Vercel permits multiple private/public stores per project. Payload provides an official @payloadcms/storage-vercel-blob adapter configurable per collection. Latest verified @vercel/blob for consistent private reads is 2.6.1.
- (event) Draft ARCHITECTURE-SPINE.md distilled from memlog; deterministic lint passed with zero findings.
- (assumption) [ASSUMPTION] The architecture spine is the only deliverable for this asynchronous pass. A separate client/team explainer is deferred until the user identifies its audience and purpose.
- (event) Async coaching pass paused for user review; spine remains draft and no assumption is marked adopted.
- (direction) Create a separate architecture review brief for one senior UI/UX engineer to challenge and recommend the frontend, backend, database, hosting, storage, analytics, and operational stack.
- (research) Wix Headless supports custom frameworks through its JavaScript SDK or REST API and has CMS permission controls. Self-managed headless retains custom Next.js/Vercel hosting but requires Wix authorization integration; Wix-managed headless uses Wix infrastructure. It is feasible but provisionally not recommended unless the organization accepts Wix coupling.
- (constraint) The user currently handles the GitHub, Vercel, Neon, domain, Email, and GA4 production accounts. This describes current administration, not yet verified organizational ownership or recovery continuity.
- (research) WordPress Core REST API can serve a separate Next.js frontend; custom post types can be exposed with show_in_rest, roles/capabilities can restrict editors, and core provides draft/pending/private statuses and revisions. The tradeoff is a separate PHP/MySQL runtime plus maintained preview, webhook, validation, security, plugin, and backup integration.
- (direction) Reopen the CMS platform choice for an explicit comparison of Payload, managed headless WordPress, and Wix Headless; do not mark Payload adopted until human review closes.
- (assumption) Candidate A (Payload) is the technical-consistency leader; Candidate B (managed headless WordPress) is the mainstream handoff leader; Candidate C (Wix Headless) remains a conditional option. Product catalog ownership stays in Git release bundles for every candidate.
- (direction) User replaced the V1 Payload/WordPress/Wix candidate decision with an adopted static-first V1 deployed on Vercel and explicitly excluded backend, CMS, database, admin auth, management APIs, and SMTP from V1.
- (decision) [ADOPTED] Git is the sole V1 publication authority for editorial content, governed catalog data, configuration, and approved web media. It binds every V1 content capability and prevents multiple authoritative stores.
- (decision) [ADOPTED] Use normal Next.js deployment on Vercel with build-time static generation, not a forced output:export constraint, so redirects, headers, default image optimization, and future platform features remain available without adding an application backend.
- (decision) [ADOPTED] About 10 series routes are generated from structured data; about 603 models are rows, with the approximately 491-row SB/SBI/SBN page as the required extreme acceptance fixture. No model gets an individual route.
- (decision) [ADOPTED] V1 recovery uses Git history, validated preview deployments, Vercel deployment promotion/rollback, and an independently retained source-media archive; there is no database restore path in V1.
- (direction) V2 backend and CMS integration must enter through backend-neutral content ports and public DTO contracts; file adapters are replaced rather than page components rewritten, and governed product data may remain file-based.
- (event) Static-first V1 architecture update distilled and reviewed: AD-1 through AD-17 remain stable, deterministic lint returned zero findings, current-tech and adversarial sequential reviews passed; spine remains draft pending team review.
